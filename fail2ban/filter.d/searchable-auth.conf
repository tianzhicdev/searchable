# Fail2Ban filter for failed authentication attempts
[Definition]
failregex = Failed login attempt.*from IP: <HOST>
            Invalid credentials.*from IP: <HOST>
            Authentication failed.*IP: <HOST>
            Unauthorized access attempt.*from <HOST>
            JWT token invalid.*from IP: <HOST>
            Login rate limit exceeded.*IP: <HOST>
ignoreregex =